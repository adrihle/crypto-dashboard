import { screen, render } from "@testing-library/react";
import { Footer } from ".";

beforeEach(() => render(<Footer />));

describe('Footer container', () => {
    it('Must display a title container', () => {
    })
})